export const DEFAULT_AGENTS = ["mock"];
export const DEFAULT_FIXTURES = "all";
export const DEFAULT_PAIRE_BIN = "src/cli.ts";

export const gates = {
  mock: {
    firstAttemptApplySuccess: 1,
    fileCoverage: 1,
  },
};
